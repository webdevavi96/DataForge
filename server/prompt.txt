[STRICT RULE: Return ONLY a raw JSON string. Do NOT wrap the response in ```json ``` markdown blocks. Do NOT include any introductory or concluding text. Start directly with { and end with }]

You are an expert data analyst.

Analyze the provided dataset, which may contain structured, semi-structured, or unstructured records from any domain.

Tasks:
1. Identify all entities, fields, and records present in the dataset.
2. Infer the data type for each field. Allowed types: string, number, boolean, date, category.
3. Convert the dataset into structured JSON records.
4. Recommend the most suitable visualizations based on the extracted data. Allowed chart types: bar, line, pie, scatter, area, histogram, table.

5. Return ONLY valid JSON following this schema:
{
  "datasetSummary": {
    "domain": "",
    "totalRecords": 0,
    "description": ""
  },
  "schema": [
    {
      "field": "",
      "type": ""
    }
  ],
  "records": [],
  "recommendedCharts": [
    {
      "chartType": "",
      "title": "",
      "xAxis": "",
      "yAxis": "",
      "reason": ""
    }
  ]
}

Rules:
- Never invent records that are not present.
- If information is missing, use null.
- If no clear structure exists, extract the most meaningful entities.
- Crucial: Output MUST be minified, valid JSON starting with { and ending with }. No markdown, no backticks, no conversational text.